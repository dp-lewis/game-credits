---
run: run-game-credits-029
work_item: expand-cluster-roster
intent: catalogue-expansion
generated: 2026-06-17T07:55:00Z
effort: low
---

# Code Review: Expand Cluster Roster

## Scope

Pure data change — 20 new entries added to the `CLUSTERS` array in `scripts/build-theme-catalogue.js`. No logic changes.

## Findings

```json
[
  {
    "file": "scripts/build-theme-catalogue.js",
    "line": 199,
    "summary": "Enemy (2013) has ~5 total cast members; may not produce 4 unique anchors per film in the villeneuve cluster",
    "failure_scenario": "verifyCluster picks a trio containing Enemy; Jake Gyllenhaal crosses to Prisoners leaving <4 exclusive anchors for Enemy → assemblePuzzle returns null → ok count drops; cluster could score ~~ or XX"
  }
]
```

## Decision

**No immediate fix applied.** `Enemy (2013)` is a known small-cast film but the failure scenario is caught by `verifyCluster` in work item 2 (`rebuild-tmdb-catalogue`). If Enemy causes the villeneuve cluster to score `~~` or `XX`, it will be swapped for `Incendies (2010)` or `Sicario` removed to make room for a larger-cast entry at that point.

All other clusters are clean:
- SLJ is properly credited in Split (not a cameo — he plays Mr. Glass in a substantial role)
- Horror cluster: A Quiet Place has sufficient named cast; Get Out and Nope share Daniel Kaluuya (good trap)
- No slug collisions within any single cluster
- No secrets, no logic changes, lint clean

## Auto-fixes Applied

None.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-game-credits-029*

---

## Work Item: rebuild-tmdb-catalogue

| Category | Auto-Fixed | Applied | Skipped |
|----------|------------|---------|---------|
| Format (prettier on JSON) | 1 | 0 | 0 |

- Catalogue is build-time data, key-free; verification reuses the production
  `assemblePuzzle`, so "OK" means the real engine accepts the trios.
- New JSON formatted with prettier (caught by the run's gate).

---

## Work Item: generate-90-day-schedule

| Category | Auto-Fixed | Applied | Skipped |
|----------|------------|---------|---------|
| Lint (no-useless-assignment) | 1 | 0 | 0 |
| Format (90 puzzle JSON) | 1 | 0 | 0 |

### Files

- `scripts/generate-schedule.js` (new) — reusable batch generator (`--start`/`--days`),
  interleaved style rotation, distinct-trio salt-retry, manifest + index rebuild.
- `public/puzzles/*.json` (90 new/regenerated) + `manifest.json` + `index.json`.

### Notes

- Fetches each cluster's casts once (~180 calls) and assembles in-process — far fewer
  TMDB calls than 90 single-date subprocesses, and deterministic per date.
- Reuses the production `assemblePuzzle`/`validatePuzzle`, so every written puzzle is a
  verified unique 3×4 trio with ≥1 trap.
- TMDB key stayed in `.env`; only public film/cast metadata written.
