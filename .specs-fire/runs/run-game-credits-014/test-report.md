---
run: run-game-credits-014
work_item: column-board-redesign
intent: call-sheet-board-redesign
generated: 2026-06-14T08:22:00Z
status: passing
---

# Test Report: Column Board Redesign

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Unit + Component | 180 | 0 | 0 | 97.3% |
| E2E (Playwright) | 6 | 0 | 0 | — |
| **Total** | **186** | **0** | **0** | **97.3%** |

## Acceptance Criteria Validation

- ✅ **N-column grid, columns = groups, labelled headers, groupSize rows** — `.headers`/`.cells` share the column template; headers show "Group n" (live 3-film = 3 columns)
- ✅ **Actors begin shuffled, evenly across columns** — `willUpdate` shuffles the cast and slices `groupSize` per column; board starts full (16/16 in the 4-film fixture, 12/12 live)
- ✅ **Select → swap → deselect** — component tests cover select, swap (two columns exchange), and tap-to-deselect
- ✅ **Swap animated via FLIP (Web Animations API, no dependency)** — `_swap`/`_flip` capture rects, mutate, `element.animate()` from the delta
- ✅ **`prefers-reduced-motion` honoured** — `matchMedia('(prefers-reduced-motion: reduce)')` short-circuits the animation (instant)
- ✅ **Keyboard accessible** — chips are native `<button>`s (Enter/Space drive the same pick path); `aria-live` status announces selection/swap
- ✅ **Submit gated to playing; grades via `gradeGroups` over columns (unchanged)** — always-full grid → submit enabled while playing; `group-logic.js` untouched
- ✅ **Solved columns lock; locked actors can't move; lives/win/lose unchanged** — component test proves a locked actor is neither selectable nor a swap target
- ✅ **Component + e2e tests updated for the new interaction** — board test rewritten; `play.spec.js` solves via swaps; `dark-mode`/`archive` selectors updated

## Tests Written

### Unit / Component Tests

- `tests/component/call-sheet-board.test.js` — column board: full-start/submit-enabled, win (any column order), game-over payload, lock+reveal-in-header, one-away hint, lose; select-then-swap, deselect, locked-immovable

### E2E Tests

- `tests/e2e/play.spec.js` — full solve through select-then-swap (reads live placement, selection-sorts into columns, submits → win); deterministic regardless of shuffle
- `tests/e2e/dark-mode.spec.js` — column header + Submit are dark-legible in dark mode
- `tests/e2e/archive.spec.js` — selector updated to the column-header text

## Test Commands

```bash
# Run all unit + component tests
npm test

# With coverage
npm run test:coverage

# E2E
npm run test:e2e
```

## Coverage Details

| Module | Statements | Branches | Functions | Lines |
|--------|------------|----------|-----------|-------|
| `All files` (src/lib gate) | 97.3% | 92.46% | 100% | 98.67% |

Components are exercised by component tests (happy-dom) but sit outside the
`src/lib` coverage gate; the gate is unchanged at 97.3%.

## Issues Found

No issues found during testing. Lint clean; production build emits 2 pages +
puzzles.

## Ready for Completion

- [x] All tests passing (180 unit/component + 6 e2e)
- [x] Coverage target met (97.3%)
- [x] All acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-game-credits-014*
