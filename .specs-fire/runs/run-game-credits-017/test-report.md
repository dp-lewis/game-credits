---
run: run-game-credits-017
work_item: board-result-reveal
intent: game-over-board-reveal
generated: 2026-06-14T13:37:00Z
status: passing
---

# Test Report: Game-Over Board Reveal (wide run)

## Work Item: board-result-reveal

### Test Results

| Category | Passed | Failed | Skipped |
|----------|--------|--------|---------|
| Unit + Component | 186 | 0 | 0 |
| E2E (Playwright) | 6 | 0 | 0 |

### Acceptance Criteria Validation

- ✅ **Win → all titles + every chip ticked; board locked** — `_revealWin` ticks all,
  headers titled, status `won`
- ✅ **Loss → ~1s beat → titles → FLIP misplaced actors into correct movies** —
  `_revealLoss` (delay, `_resolveColumnFilms`, target columns, reused `_flip`)
- ✅ **After loss, correct final columns; ticks only on player-correct picks** —
  component test asserts each actor in its film's column and the exact tick set
- ✅ **Old lost-state text answer list removed** — `_renderSolution` + `.solution`
  CSS deleted
- ✅ **`prefers-reduced-motion` → instant** — delay/FLIP/cascade all guarded
- ✅ **Static `reveal` mode → solved board, no ticks, no submit/lives** — component
  test asserts titles, no ticks, no submit/lives elements
- ✅ **Tick decoupled from `locked`** — new `ticked` prop drives ✓; `locked` only
  disables

### Tests Written

- `tests/component/call-sheet-board.test.js` — `game-over reveal` suite: win (all
  ticked + titles), loss (correct columns + exact tick set + titles), static reveal
  (titles, no ticks, no submit/lives). Helper `revealDelayMs = 0` + `await
  board._revealDone` keep tests fast and deterministic.

### Notes

Item 1 keeps the existing `play.spec.js` win assertions (Copy result / share grid)
green because the result card is still mounted; item 2 (`retire-result-card`)
removes it and re-points that e2e.

## Test Commands

```bash
npm test && npm run test:e2e
npm run lint && npm run build
```

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-game-credits-017*

---

## Work Item: retire-result-card

### Test Results

| Category | Passed | Failed | Skipped |
|----------|--------|--------|---------|
| Unit + Component | 182 | 0 | 0 |
| E2E (Playwright) | 6 | 0 | 0 |

(182 = 186 − 4 from the deleted `call-sheet-result.test.js`.)

### Acceptance Criteria Validation

- ✅ **`<call-sheet-result>` removed from the app; component + test deleted**
- ✅ **Nothing renders below the board on game over** — result block gone; board self-reveals
- ✅ **Already-played restore shows the static-reveal board + "already played" note** — app `_played` branch renders `<call-sheet-board .reveal>` + the note
- ✅ **Streak still recorded** — `_onGameOver` keeps `recordResult(... {updateStreak})`; only the display is gone
- ✅ **`play.spec.js` asserts the on-board win reveal** — banner + 12 tick spans + no Submit/Copy-result
- ✅ **Suite + e2e green; coverage held (97.3%); build OK**

### Notes

`src/lib/share-grid.js` + its two unit tests retained (pure, tested, re-addable).
