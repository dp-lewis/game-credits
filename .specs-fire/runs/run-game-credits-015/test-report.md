---
run: run-game-credits-015
work_item: per-group-progress
intent: call-sheet-board-redesign
generated: 2026-06-14T12:40:00Z
status: passing
---

# Test Report: Per-Group Progress Feedback

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Unit + Component | 183 | 0 | 0 | 97.x% |
| E2E (Playwright) | 6 | 0 | 0 | — |
| **Total** | **189** | **0** | **0** | — |

## Acceptance Criteria Validation

- ✅ **Each group shows correct count out of total (`n/4`) on submit** — `_progress`
  built from `gradeGroups`' new `correctCount`; rendered as `Grp{n} {count}/{total}`
- ✅ **A fully-correct group reads `4/4 ✓`** — solved groups carry `solved: true` and
  render the `✓` + green colour
- ✅ **Old single "One away…" line removed entirely** — `_oneAway`, `_renderHint`,
  and the `.hint` element are gone; no remaining references in `src/`
- ✅ **Feedback updates on every submit, reflecting the current arrangement** —
  `_submit` recomputes `_progress` each time
- ✅ **Readable in light/dark** — neutral text + `--cs-correct` for solved; inherits
  the themed palette
- ✅ **Announced for screen readers** — progress sits in a `role="status"`
  `aria-live="polite"` element
- ✅ **Component tests cover the per-group counts (all-correct, partial, zero/none)**
  — replaced the `oneAway` assertions; added a "no line before first submit" case
- ✅ **`gradeGroups` grading semantics unchanged** — only an additive `correctCount`
  field; `correct`/`oneAway`/`filmId`/`allSolved` untouched (existing unit tests pass)

## Tests Written

### Unit Tests

- `tests/unit/group-logic.test.js` — `correctCount` for a solved group (4),
  per-group modal counts `[4,3,2,1]`, and a modal count in an under-filled bucket

### Component Tests

- `tests/component/call-sheet-board.test.js` — progress line shows `Grp1 4/4 ✓ ·
  Grp2 3/4 · Grp3 2/4` after submit (state + rendered text); no progress line before
  the first submit

## Test Commands

```bash
npm test            # 183 unit + component
npm run test:e2e    # 6 e2e
npm run lint && npm run build
```

## Coverage Details

`src/lib/group-logic.js` is fully exercised (added `correctCount` covered by new
assertions). Overall `src/lib` gate remains ~97%+, unchanged threshold.

## Issues Found

One self-inflicted test-fixture bug during development (a column layout that wasn't
a valid 16-actor partition → undefined placement); fixed by using a proper
permutation. No product issues.

## Ready for Completion

- [x] All tests passing (183 unit/component + 6 e2e)
- [x] Coverage target met
- [x] All acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-game-credits-015*
