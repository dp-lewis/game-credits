---
run: run-game-credits-016
work_item: header-feedback-movie-labels
intent: call-sheet-board-redesign
generated: 2026-06-14T12:58:00Z
status: passing
---

# Test Report: Header Feedback and Movie Labels

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Unit + Component | 183 | 0 | 0 | ~97% |
| E2E (Playwright) | 6 | 0 | 0 | — |
| **Total** | **189** | **0** | **0** | — |

## Acceptance Criteria Validation

- ✅ **Separate progress line removed; count renders inside each header** — `.progress`
  element + `_renderProgress` deleted; `_renderHeaders` shows the per-column count
- ✅ **Unsolved headers read "Movie n"; show `n/total` after a submit** — `<span
  class="label">Movie n</span>` + `<span class="count">` when `_progress` set
- ✅ **No count before the first submit** — count span only renders when
  `_progress?.[c]` exists; tested
- ✅ **Solved header shows the movie title + tick (✓)** — `<span class="title">` +
  `<span class="tick">✓</span>`; tested
- ✅ **Header row is a live region; legible in light + dark** — `.headers` is
  `role="status" aria-live="polite"`; dark-mode e2e asserts the header surface stays
  dark
- ✅ **Component + e2e tests updated** — header-based progress assertions; e2e label
  selectors "Group n" → "Movie n"

## Tests Written / Updated

### Component Tests

- `tests/component/call-sheet-board.test.js` — solved header has title + `.tick`;
  per-group `n/4` read from each header's `.count`; "Movie n" with no count before
  the first submit

### E2E Tests

- `tests/e2e/play.spec.js`, `tests/e2e/archive.spec.js` — "Movie 1" label
- `tests/e2e/dark-mode.spec.js` — targets the `.head` surface filtered by "Movie 2"

## Test Commands

```bash
npm test && npm run test:e2e
npm run lint && npm run build
```

## Issues Found

None. Lint clean; build emits 2 pages + puzzles.

## Ready for Completion

- [x] All tests passing (183 unit/component + 6 e2e)
- [x] All acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-game-credits-016*
