# Copy to .env (gitignored) and fill in. Build-time only — never shipped to the client.

# TMDB v3 API key, used by `npm run build:puzzle` in live mode to source casts.
# Get one free at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
